{
  "name": "mcp-server",
  "version": "1.0.0",
  "description": "Server MCP per Claude",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "DEBUG=true node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["mcp", "claude", "server"],
  "author": "sergeguea",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
